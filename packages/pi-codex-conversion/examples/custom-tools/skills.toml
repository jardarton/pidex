usage = '''await tools.skills("list" | "read <skill> [reference-name...]") // At session start list once, then read skills marked always apply and task-relevant skills before work'''
description = "List or read skills"
output = "Skill catalog, instructions with paths, or requested references"
command = "./skills/skills.mjs"
input = "stdin"
defer_loading = false
